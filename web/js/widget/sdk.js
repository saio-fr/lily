/* jshint strict: false */
var sdk = (function() {

  if (sdk) return sdk;

  return {
    config: config,
    api: api
  };

  function config(name, obj) {
    // trigger xdm event with config
  }

  function api(name, obj) {
    // trigger xdm event with api
  }

})();
