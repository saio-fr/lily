/**************************************************
Button to display the merge button. En attente de la prestation du CNRS.
// ***************************************************/
lily.mergeView =lily.standardHeaderView.extend({
    template: _.template($('#merge-button').html()),
});