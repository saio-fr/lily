<% if (questions && !_.isEmpty(questions)) { %>
<h3 class="on-boarding-title on-boarding-title-avi">
  <%= onBoarding.title %>
</h3>
<ul class="top-questions-list">
  <% _.each(questions, function(question) { %>
  <li class="top-question-item">
    <a class="top-question-link" href="javascript:;" data-question-id="<%= question.id %>"><%= question.title %></a>
  </li>
  <% }); %>
</ul>
<% } else { %>
<h3 class="on-boarding-title on-boarding-title-avi">
  <%= onBoarding.empty %>
</h3>
<% } %>
