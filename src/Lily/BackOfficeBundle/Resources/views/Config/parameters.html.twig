
  <header class="header bg-dark bg-gradient">
    <a id="save" href="#" class="btn btn-success pull-right">Sauvegarder</a>
    <ul class="nav nav-tabs">
      <li class="active"><a href="#overall" data-toggle="tab">Globaux</a></li>
      <!-- show if enterprise.avi is enabled -->
      {% if app.user.enterprise.avi %}
      <li class=""><a href="#avatar" data-toggle="tab">Avatar</a></li>
      {% endif %}
      <!-- show if enterprise.chat is enabled -->
      {% if app.user.enterprise.chat %}
      <li class=""><a href="#chat" data-toggle="tab">Live chat</a></li>
	  {% endif %}
    </ul>
  </header>     
  <section class="scrollable wrapper">
    <div class="tab-content">
    
      <!-- OVERALL -->
      <section class="tab-pane active" id="overall">          
        <h5>Fonctionnalités</h5>
        <div class="row">
          <label class="switch">
            <input name="maintenance" type="checkbox" <% if (!maintenance) { %> checked <% } %> >
              <span></span>
          </label>
          <p>Afficher l'iframe</p>
        </div>
        
        <!-- show if enterprise.chat is enabled -->
        {% if app.user.enterprise.chat and app.user.enterprise.avi %}
        <p class="administration-p">Choisissez votre canal de communication par défaut :</p>
        <div class="row">
        	<p>Avatar virtuel</p>
        	<label class="switch no-color">
            	<input name="home" type="checkbox" <% if (home == 'chat') { %> checked <% } %>>
            	<span></span>
            </label>
            <p>Live chat</p>
        </div>
        {% endif %}
        
        <!-- show if enterprise.chat && enterprise.avi is enabled -->
        {% if app.user.enterprise.chat and app.user.enterprise.avi %}
        <div class="checkbox">
          <label class="checkbox-custom">
            <input type="checkbox" name="aviIfNoOperator" <% if (aviIfNoOperator) { %> checked <% } %> >
            <i class="icon-unchecked"></i>
            Proposer directement l'AVI si aucun conseiller n'est disponible
          </label>
        </div>
        {% endif %}
        
        <h5 class="m-t-xlg">Activer / Désactiver un service</h5>
        <div class="row">
        	
        	<!-- show if enterprise.chat is enabled -->
        	{% if app.user.enterprise.chat %}
        	<div class="return">
            	<label class="switch">
                	<input type="checkbox" name="chat" <% if (chat) { %> checked <% } %>>
                	<span></span>
                </label>
                <p>Live chat</p>
            </div>
            {% endif %}
            
            <!-- show if enterprise.faq is enabled -->
            {% if app.user.enterprise.faq %}
            <div class="return">
                <label class="switch">
                	<input type="checkbox" name="faq" <% if (faq) { %> checked <% } %>>
                	<span></span>
                </label>
                <p>FAQ dynamique</p>
            </div>
            {% endif %}
            
            <!-- show if enterprise.topQuestions is enabled -->
            {% if app.user.enterprise.topQuestions %}
            <div class="return">
                <label class="switch">
                	<input type="checkbox" name="topQuestions" <% if (topQuestions) { %> checked <% } %>>
                	<span></span>
                </label>
                <p>Top des questions</p>
            </div>
			{% endif %}
			
			<div class="return">
                <label class="switch">
                	<input name="redirectionMail" type="checkbox" <% if (redirectionMail) { %> checked <% } %> >
                	<span></span>
                </label>
                <p>Redirection par Mail</p>
            </div>
			<div class="return">
            	<label class="switch">
                	<input name="redirectionTel" type="checkbox" <% if (redirectionTel) { %> checked <% } %> >
                	<span></span>
                </label>
                <p>Redirection par Téléphone</p>
            </div>
			
        </div>
      </section>
      
      <!-- AVATAR -->
      <section class="tab-pane" id="avatar">
        <h5>Message d'accueil</h5>
        <div class="col-sm-12">
          <div id="editor" name="aviWelcomeMsg" class="form-control" style="overflow:scroll;height:50px;max-height:150px;" contenteditable="true"><%= aviWelcomeMsg %></div>
        </div>
        <h5 class="m-t-lg">Animations</h5>
        <div class="row">
            <label class="switch">
            	<input name="aviAnimations" type="checkbox" <% if (aviAnimations) { %> checked <% } %> >
            	<span></span>
            </label>
            <p>Activer les animations</p>
    	</div>
      </section>
      
      <!-- CHAT -->
      <section class="tab-pane" id="chat">
      	<h5>Conversations</h5>
		<div class="row">
            <label class="switch">
            	<input name="chatAutoSetOperator" type="checkbox" <% if (chatAutoSetOperator) { %> checked <% } %> >
            	<span></span>
            </label>
            <p>Attribuer automatiquement un opérateur s'il est disponible</p>
    	</div>
    	
	    <p class="administration-p">Limiter le nombre de conversations à</p>
    	<input name="chatMax" type="text" class="form-control chatMax" value="<%= chatMax %>">
    	<p class="administration-p">par opérateur disponible</p>
    	
        <h5>File d'attente</h5>
		<div class="row">
            <label class="switch">
            	<input name="chatQueue" type="checkbox" <% if (chatQueue) { %> checked <% } %> >
            	<span></span>
            </label>
            <p>Activer la file d'attente</p>
    	</div>
    	<div>
        	<p class="administration-p">Limiter la file d'attente à</p>
        	<input name="chatMaxQueue" type="text" class="form-control chatMaxQueue" value="<%= chatMaxQueue %>">
        	<p class="administration-p">places par conseiller en production</p>
    	</div>
    	
    	<h5>Ecran d'accueil</h5>
		<div class="row">
            <label class="switch">
            	<input name="chatContactForm" type="checkbox" <% if (chatContactForm) { %> checked <% } %> >
            	<span></span>
            </label>
            <p>Activer un écran d'accueil invitant l'utilisateur à renseigner ses informations personnelles</p>
    	</div>
    	<p class="administration-p">Message d'accueil :</p>
    	<div class="col-sm-12">
          <div id="editor" name="chatContactFormMsg" class="form-control" style="overflow:scroll;height:50px;max-height:150px;" contenteditable="true"><%= chatContactFormMsg %></div>
        </div>
    	<p class="administration-p">Afficher sur l'écran d'accueil :</p>
        <div class="row">
        	<div class="return">
            	<label class="switch">
                	<input name="chatContactLastNameField" type="checkbox" <% if (chatContactLastNameField) { %> checked <% } %> >
                	<span></span>
                </label>
                <p>Un champ nom</p>
            </div>
            <div class="return">
                <label class="switch">
                	<input name="chatContactFirstNameField" type="checkbox" <% if (chatContactFirstNameField) { %> checked <% } %> >
                	<span></span>
                </label>
                <p>Un champ prénom</p>
            </div>
            <div class="return">
                <label class="switch">
                	<input name="chatContactEmailField" type="checkbox" <% if (chatContactEmailField) { %> checked <% } %>>
                	<span></span>
                </label>
                <p>Un champ email</p>
            </div>
        </div>
        <div class="row">
            <label class="switch">
            	<input name="chatContactFormAvoidable" type="checkbox" <% if (chatContactFormAvoidable) { %> checked <% } %> >
            	<span></span>
            </label>
            <p>Autoriser le visiteur à ne pas renseigner ces informations</p>
    	</div>
      </section>
    </div>
  </section>               